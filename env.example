# Database Configuration (Railway otomatik verir)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Session Secret (Railway'de manuel ekle)
SESSION_SECRET=change-this-to-a-very-strong-random-secret-key

# Environment
NODE_ENV=production

# Node Version
NODE_VERSION=20

# Server Port (Railway otomatik verir)
PORT=5000
